# my-app-name

## Installation

```bash
cp .env.dist .env
cp .env.dist .env.production
cp .env.dist .env.staging

yarn
```

## Running the app

```bash
# development
yarn dev

# staging
yarn staging

# production
yarn build && yarn prod

# production deploy
yarn deploy
```
